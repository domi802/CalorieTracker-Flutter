import 'package:flutter/material.dart';

class recipeScreen extends StatelessWidget {
  const recipeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Recipe Screen")));
  }
}
